[pytest]
addopts = -v -ra --tb=short
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: Unit tests
    integration: Integration tests
    persona: Tests for Gonzo's personality and responses
    legal: Tests for legal analysis capabilities
    ml: Tests for machine learning components